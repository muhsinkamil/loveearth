import { motion } from 'framer-motion';
import './styles.scss';

const LoadingScreen = () => {
  return (
    <div className="loading-container">
      <span className="loading-logo">LightHouse</span>
    </div>
  );
};

export default LoadingScreen;
